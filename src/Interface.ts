export const BODY = 'body';

export enum chromeDirection {
  right = 'ArrowRight',
  left = 'ArrowLeft',
  up = 'ArrowUp',
  down = 'ArrowDown'
}
export enum ieDirection {
  right = 'Right',
  left = 'Left',
  up = 'Up',
  down = 'Down',
}

export interface Direction {
  
}